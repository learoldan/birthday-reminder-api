handler: src/verifyOrCreateUser/index.handler
description: check if a user exist and if not create it
events:
    - httpApi:
          path: /user
          method: POST
          authorizer: auth0Authorizer
